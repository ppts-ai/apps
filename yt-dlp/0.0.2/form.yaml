type: object
title: yt-dlp
version: 0.0.2
description: "most popular video/audio downloader"
instructions: "Help the user manage a yt-dlp commandline argument form. If the user provides some requirement, help the user fill in the form with proper argument"
labels:
  title: "yt-dlp command Copilot"
  initial: "Hi you! 👋 I can help you fill in the form."
groups:
  - name: "basic params"
    fields:
      - url
  - name: "network params"
    fields:
      - output
      - verbose
properties:
  url:
    type: string
    title: "Url"
    description: "the download url"
  output:
    type: string
    title: "Output"
    description: "output folder"
  verbose:
    type: boolean
    title: "no-check-certificates"
    default: true
    description: "--no-check-certificates"
    template:
      - "--no-check-certificates"
